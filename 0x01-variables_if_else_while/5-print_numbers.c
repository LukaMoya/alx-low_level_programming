#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - prints 0123456789
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%i", n);
		i++;
	}
	putchar('\n');
	return (0);
}
